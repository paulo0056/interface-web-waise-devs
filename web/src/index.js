import React from 'react';// importação para se usar tags html com java script
import ReactDOM from 'react-dom';// importação pra poder usar a arvore de elementos do html com o react
import App from './App';


ReactDOM.render(<App />, document.getElementById('root'));// ele ta mandando o conteudo de app para o root

// JSX (javascript +  XML)
